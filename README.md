# HackerNews
HackerNews using both Algolia's API and HackerNews' own API. The fetched stories are persisted, using redux-persist. The story details and the first initial comments(without the replies) are persisted simply using localStorage.
